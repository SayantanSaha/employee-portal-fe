import { ParseChangedDataPipe } from './parse-changed-data.pipe';

describe('ParseChangedDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseChangedDataPipe();
    expect(pipe).toBeTruthy();
  });
});
